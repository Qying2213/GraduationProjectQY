# 智能招聘系统 CI/CD 流水线指南

## 1. 概述

本项目使用 **GitHub Actions** 实现 CI/CD 自动化流水线，包括：
- 代码质量检查
- 自动化测试
- Docker 镜像构建
- 自动部署到服务器

## 2. 项目架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        GitHub Repository                         │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      GitHub Actions CI/CD                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │
│  │  Lint    │→ │  Test    │→ │  Build   │→ │     Deploy       │ │
│  │  检查    │  │  测试    │  │  构建    │  │     部署         │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Docker Hub / 阿里云镜像                     │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────────┐ │
│  │ gateway:latest │  │ user-service   │  │ resume-service     │ │
│  │ job-service    │  │ talent-service │  │ recommendation-svc │ │
│  └────────────────┘  └────────────────┘  └────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Production Server                           │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    Docker Compose                         │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐ │   │
│  │  │ Gateway │ │ Services│ │PostgreSQL│ │     Redis      │ │   │
│  │  │  :8080  │ │ :8081-87│ │  :5432  │ │     :6379      │ │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────────────┘ │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## 3. 服务列表

| 服务名称 | 端口 | 描述 |
|---------|------|------|
| gateway | 8080 | API 网关 |
| user-service | 8081 | 用户服务 |
| job-service | 8082 | 职位服务 |
| interview-service | 8083 | 面试服务 |
| resume-service | 8084 | 简历服务 |
| message-service | 8085 | 消息服务 |
| talent-service | 8086 | 人才服务 |
| recommendation-service | 8087 | 推荐服务 |
| frontend | 80 | 前端应用 |

## 4. CI/CD 流程

### 4.1 触发条件
- `push` 到 `main` 或 `develop` 分支
- 创建 `Pull Request` 到 `main` 分支
- 手动触发 (`workflow_dispatch`)

### 4.2 流水线阶段

```
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│  Lint   │ →  │  Test   │ →  │  Build  │ →  │  Push   │ →  │ Deploy  │
│ 代码检查 │    │ 单元测试 │    │ 构建镜像 │    │ 推送镜像 │    │ 部署服务 │
└─────────┘    └─────────┘    └─────────┘    └─────────┘    └─────────┘
```

## 5. 文件结构

```
buildANDdeployment/
├── 01-CICD概述.md              # 本文档
├── 02-GitHub-Actions配置.md    # GitHub Actions 详细配置
├── 03-Docker部署指南.md        # Docker 部署说明
├── 04-服务器部署指南.md        # 服务器配置说明
└── 05-常见问题.md              # FAQ

.github/
└── workflows/
    ├── ci.yml                  # CI 流水线
    ├── cd.yml                  # CD 流水线
    └── test.yml                # 测试流水线
```

## 6. 快速开始

1. Fork 本仓库到你的 GitHub 账号
2. 配置 GitHub Secrets（见下一章节）
3. 推送代码触发流水线
4. 查看 Actions 页面监控构建状态

## 7. 下一步

请继续阅读：
- [02-GitHub-Actions配置.md](./02-GitHub-Actions配置.md) - 详细配置说明
- [03-Docker部署指南.md](./03-Docker部署指南.md) - Docker 相关配置
- [04-服务器部署指南.md](./04-服务器部署指南.md) - 生产环境部署

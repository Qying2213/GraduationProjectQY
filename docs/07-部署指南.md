# 部署指南

## 环境要求

### 硬件要求
- CPU: 4核+
- 内存: 8GB+
- 磁盘: 50GB+

### 软件要求
- Go 1.21+
- Node.js 18+
- PostgreSQL 14+
- Redis 7+
- Elasticsearch 8+ (可选)
- Docker & Docker Compose (推荐)

## 快速部署 (Docker Compose)

### 1. 配置环境变量

```bash
cp backend/.env.example backend/.env
```

编辑 `.env` 文件：
```bash
# 数据库配置
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=talent_platform

# Redis配置
REDIS_HOST=redis
REDIS_PORT=6379

# JWT配置
JWT_SECRET=your_jwt_secret

# Coze配置
COZE_API_TOKEN=your_coze_token
COZE_WORKFLOW_ID=your_workflow_id

# 火山引擎配置
VOLC_ACCESS_KEY=your_access_key
VOLC_SECRET_KEY=your_secret_key
```

### 2. 启动服务

```bash
docker-compose up -d
```

### 3. 初始化数据库

```bash
./init-db.sh
```

### 4. 访问服务

- 前端: http://localhost:3000
- API网关: http://localhost:8080
- 各微服务: 8081-8089

## 手动部署

### 1. 安装依赖

```bash
# 安装Go依赖（按服务拉取）
for svc in gateway user-service job-service interview-service resume-service message-service talent-service recommendation-service evaluator-service; do
  (cd backend/$svc && go mod tidy)
done

# 安装前端依赖
cd frontend
npm install
```

### 2. 配置数据库

```sql
-- 创建数据库
CREATE DATABASE talent_platform;

-- 创建用户
CREATE USER talent_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE talent_platform TO talent_user;
```

### 3. 启动后端服务

```bash
# 启动所有后端服务
./start-all.sh

# 或单独启动
cd backend/gateway && go run main.go &
cd backend/user-service && go run main.go &
cd backend/job-service && go run main.go &
cd backend/interview-service && go run main.go &
cd backend/resume-service && go run main.go &
cd backend/message-service && go run main.go &
cd backend/talent-service && go run main.go &
cd backend/recommendation-service && go run main.go &
cd backend/log-service && go run main.go &
cd backend/evaluator-service && go run cmd/server/main.go &
```

### 4. 启动前端

```bash
cd frontend
npm run dev
```

## 生产环境部署

### Nginx配置

```nginx
upstream gateway {
    server 127.0.0.1:8080;
}

upstream frontend {
    server 127.0.0.1:3000;
}

server {
    listen 80;
    server_name your-domain.com;

    # 前端
    location / {
        proxy_pass http://frontend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # API
    location /api/ {
        proxy_pass http://gateway;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # WebSocket
    location /ws/ {
        proxy_pass http://gateway;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

### Systemd服务配置

```ini
# /etc/systemd/system/talent-gateway.service
[Unit]
Description=Talent Platform Gateway
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/talent-platform/backend/gateway
ExecStart=/opt/talent-platform/backend/gateway/gateway
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

### 健康检查

```bash
# 检查服务状态
curl http://localhost:8080/health

# 检查各微服务
curl http://localhost:8081/health  # user-service
curl http://localhost:8082/health  # job-service
curl http://localhost:8086/health  # resume-service
```

## 性能调优

### Go服务优化

```go
// 设置GOMAXPROCS
runtime.GOMAXPROCS(runtime.NumCPU())

// 数据库连接池
db.SetMaxOpenConns(100)
db.SetMaxIdleConns(10)
db.SetConnMaxLifetime(time.Hour)
```

### Redis配置

```conf
maxmemory 1gb
maxmemory-policy allkeys-lru
```

### PostgreSQL配置

```conf
shared_buffers = 2GB
effective_cache_size = 6GB
work_mem = 64MB
maintenance_work_mem = 512MB
```

## 监控告警

### Prometheus指标

```yaml
# prometheus.yml
scrape_configs:
  - job_name: 'talent-platform'
    static_configs:
      - targets: ['localhost:8080', 'localhost:8081', 'localhost:8086']
```

### 日志收集

```yaml
# filebeat.yml
filebeat.inputs:
  - type: log
    paths:
      - /var/log/talent-platform/*.log
output.elasticsearch:
  hosts: ["localhost:9200"]
```

## 故障排查

### 常见问题

1. **服务启动失败**
   - 检查端口占用: `lsof -i :8080`
   - 检查日志: `journalctl -u talent-gateway`

2. **数据库连接失败**
   - 检查连接字符串
   - 检查防火墙规则

3. **AI服务超时**
   - 检查网络连接
   - 检查API Token有效性

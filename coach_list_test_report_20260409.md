# 教练列表模块测试报告

## 基本信息

- 测试日期: 2026-04-09
- 测试范围: 教练管理 -> 教练列表
- 测试设计来源: `/Users/qinyang/Desktop/ceshiyongli/教练列表测试执行思维导图.xmind`
- 执行配置: `/Users/qinyang/Desktop/ceshiyongli/coach_web_test_config.toml`
- 执行工具: `/Users/qinyang/Desktop/ceshiyongli/run_coach_web_tests`
- 测试接口基址: `https://test-table-coach.motern.com/api/admin/v1`

## 结论摘要

- 本次共执行 11 条自动化用例
- 通过 9 条
- 失败 2 条
- 当前自动化已覆盖 XMind 中 26 个列表测试点里的 14 个
- 其余 12 个测试点需要浏览器 UI 自动化或人工验证，当前这个 API runner 无法直接执行

## 自动化覆盖映射

### 已自动化覆盖的 XMind 测试点

1. 首次进入列表
2. 空结果状态
3. 封面图为空占位的数据前置校验
4. 姓名与对外展示姓名字段区分
5. 状态与展示区分
6. 按教练ID搜索
7. 按非法ID搜索
8. 按姓名搜索
9. 按不存在姓名搜索
10. 多条件组合搜索
11. 正常分页
12. `page=0`
13. 超大页码
14. `limit=0`

### 当前 runner 无法直接执行的 XMind 测试点

1. 加载失败提示
2. 常驻区域过长展示
3. 横向滚动可用性
4. 点击重置
5. `limit` 超大时是否存在服务端上限控制
6. 关联课程为 0
7. 关联门店为 0
8. 点击关联课程
9. 点击关联门店
10. 查看详情
11. 切换状态 有效/禁用
12. 切换展示 展示/隐藏

说明:

- `limit` 超大这个点当前没有自动化落地，是因为现网总数只有 30 条；即使服务端做了 `100` 的上限控制，返回结果仍可能是 30 条，单靠当前列表接口无法区分“做了上限控制”和“没做上限控制”。
- `course_count=0` / `site_count=0` 在本次在线数据里没有找到可用样本，缺少自动化前置数据。

## 用例结果

| 序号 | 用例名称 | 结果 | 说明 |
|---|---|---|---|
| 1 | 教练列表_默认加载_第一页数据正常 | PASS | 返回 `10` 条，`total=30`，首条记录为 `test` |
| 2 | 教练列表_空结果_不存在姓名 | PASS | `name=不存在的教练XYZ` 返回空列表，`total=0` |
| 3 | 教练列表_按ID精确搜索_唯一命中 | PASS | `id=2041821272066371584` 唯一命中 `test` |
| 4 | 教练列表_按非法ID搜索_空列表且不报错 | PASS | `id=abc` 返回空列表，接口未报错 |
| 5 | 教练列表_按姓名搜索_姓名展示名封面图校验 | PASS | `name=诗诗` 唯一命中，`show_name=樊烽华`，`header_image` 为空 |
| 6 | 教练列表_多条件组合搜索_唯一命中诗诗 | PASS | `type=1, level=3, sex=1, show_status=1` 唯一命中 `诗诗` |
| 7 | 教练列表_状态与展示区分_隐藏记录仍有效 | PASS | `name=薛文城` 命中记录 `status=1` 且 `show_status=2` |
| 8 | 教练列表_按展示状态筛选_返回两条隐藏记录 | PASS | `show_status=2` 返回 2 条，首条城市为 `深圳市` |
| 9 | 教练列表_超大页码_返回空列表总数不变 | PASS | `page=999` 返回空列表，`total` 仍为 `30` |
| 10 | 教练列表_分页边界_page0_应友好报错 | FAIL | 返回内容不是单个合法 JSON，暴露接口错误处理问题 |
| 11 | 教练列表_分页边界_limit0_不应直接返回全部数据 | FAIL | `limit=0` 实际返回全部 `30` 条数据 |

## 失败项与缺陷

### 1. `page=0` 返回异常响应体

- 对应用例: `教练列表_分页边界_page0_应友好报错`
- 实际结果: runner 报错 `响应不是 JSON: invalid character '{' after top-level value`
- 补充观察: 手工探测时接口返回了 `400 参数错误`，但响应体后面还拼接了一段成功 JSON，导致整体不是合法单个 JSON
- 风险:
  - 前端或调用方难以稳定解析
  - 属于典型的错误处理分支重复写响应问题

### 2. `limit=0` 直接返回全部数据

- 对应用例: `教练列表_分页边界_limit0_不应直接返回全部数据`
- 期望结果: 参数校验失败，或返回空结果，不应直接全量放开
- 实际结果: `total=30`，`returned_count=30`
- 风险:
  - 分页接口边界失效
  - 数据量增大后可能引起性能问题
  - 前端可绕过分页约束一次性拉全量数据

## 测试说明

- 本次自动化完全基于当前现有的 `run_coach_web_tests` runner 执行，未引入浏览器自动化框架
- 为保证稳定性，本次配置将 AI 校验关闭，全部使用确定性断言
- 配置文件末尾已保留“无法被当前 runner 自动执行”的 XMind 测试点说明，便于后续扩展到 Playwright / Selenium 时继续补齐

## 建议下一步

1. 先修复列表接口的两个分页边界缺陷: `page=0`、`limit=0`
2. 如果你要继续把 XMind 剩余点自动化，建议下一阶段引入浏览器级测试，优先覆盖:
   - 点击重置
   - 查看详情
   - 切换状态/展示
   - 关联课程 / 关联门店弹窗
   - 横向滚动与空状态展示

# 智能人才运营平台 - 前端

基于 Vue 3 + TypeScript + Vite + Element Plus 构建的现代化前端应用

## 技术栈

- **框架**: Vue 3 (Composition API)
- **语言**: TypeScript 5+
- **构建工具**: Vite 5
- **UI 组件**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router 4
- **HTTP 客户端**: Axios
- **图表**: ECharts 5
- **样式**: SCSS

## 快速开始

### 安装依赖

```bash
cd frontend
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 项目结构

```
frontend/
├── src/
│   ├── api/                  # API 接口
│   │   ├── auth.ts          # 认证接口
│   │   ├── talent.ts        # 人才接口
│   │   ├── job.ts           # 职位接口
│   │   ├── recommendation.ts # 推荐接口
│   │   └── message.ts       # 消息接口
│   ├── assets/              # 静态资源
│   ├── components/          # 公共组件
│   │   ├── common/          # 通用组件
│   │   ├── layout/          # 布局组件
│   │   │   └── MainLayout.vue
│   │   └── charts/          # 图表组件
│   ├── views/               # 页面视图
│   │   ├── auth/            # 认证页面
│   │   │   ├── Login.vue
│   │   │   └── Register.vue
│   │   ├── dashboard/       # 仪表板
│   │   │   └── Dashboard.vue
│   │   ├── talents/         # 人才管理
│   │   │   ├── TalentList.vue
│   │   │   └── TalentDetail.vue
│   │   ├── jobs/            # 职位管理
│   │   │   ├── JobList.vue
│   │   │   └── JobDetail.vue
│   │   ├── resumes/         # 简历管理
│   │   ├── recommend/       # 推荐系统
│   │   ├── messages/        # 消息中心
│   │   └── profile/         # 个人中心
│   ├── router/              # 路由配置
│   │   └── index.ts
│   ├── store/               # 状态管理
│   │   └── user.ts          # 用户状态
│   ├── utils/               # 工具函数
│   │   └── request.ts       # HTTP 请求封装
│   ├── styles/              # 全局样式
│   │   └── global.scss
│   ├── types/               # TypeScript 类型定义
│   │   └── index.ts
│   ├── App.vue
│   └── main.ts
├── public/
├── index.html
├── vite.config.ts
├── tsconfig.json
└── package.json
```

## 功能模块

### 1. 用户认证
- 登录 / 注册
- JWT Token 管理
- 角色权限控制（管理员、HR、候选人）

### 2. 仪表板
- 数据统计卡片
- ECharts 可视化图表
- 最近活动时间线

### 3. 人才管理
- 人才列表（搜索、筛选、分页）
- 人才详情
- 新增 / 编辑 / 删除人才

### 4. 职位管理
- 职位列表
- 职位发布 / 编辑
- 职位状态管理

### 5. 简历管理
- 简历上传
- 简历解析
- 申请管理

### 6. 智能推荐
- 为人才推荐职位
- 为职位推荐人才
- 匹配度展示

### 7. 消息中心
- 站内消息
- 未读消息提醒
- 消息已读标记

### 8. 个人中心
- 用户信息编辑
- 头像上传
- 密码修改

## 设计特色

- 🎨 **现代化 UI**: 采用渐变色、卡片设计、微动画
- 📱 **响应式布局**: 适配各种屏幕尺寸
- 🚀 **性能优化**: 路由懒加载、组件按需引入
- 🎯 **用户体验**: 加载状态、错误提示、操作反馈
- 🔒 **安全性**: JWT 认证、路由守卫、权限控制

## API 接口

所有 API 请求通过代理转发到后端网关 `http://localhost:8080/api/v1`

### 认证接口
- POST `/login` - 登录
- POST `/register` - 注册
- GET `/profile` - 获取用户信息

### 人才接口
- GET `/talents` - 获取人才列表
- POST `/talents` - 创建人才
- GET `/talents/:id` - 获取人才详情
- PUT `/talents/:id` - 更新人才
- DELETE `/talents/:id` - 删除人才

### 职位接口
- GET `/jobs` - 获取职位列表
- POST `/jobs` - 创建职位
- GET `/jobs/stats` - 职位统计

### 推荐接口
- POST `/recommendations/jobs-for-talent` - 为人才推荐职位
- POST `/recommendations/talents-for-job` - 为职位推荐人才

### 消息接口
- GET `/messages` - 获取消息列表
- POST `/messages` - 发送消息
- GET `/messages/unread-count` - 获取未读数

## 开发规范

- 使用 TypeScript 严格模式
- 组件使用 Composition API + `<script setup>`
- 遵循 Vue 3 官方风格指南
- 使用 SCSS 编写样式
- API 请求统一使用封装的 request 实例
